# RCC (Rust C Compiler)

rcc is a transpiler for C. It takes in a C sourcefile, outputs a temporary rust file and uses your system rust compiler to compile it.

This FORCES developers to write memory safe code, complying with rust's safe memory management.
